# Spring Boot configuration
spring:
  application:
    name: discovery # Specifies the name of the Spring Boot application

  # Import the externalized configuration from the Config Server
  config:
    import: optional:configserver:http://localhost:8888
